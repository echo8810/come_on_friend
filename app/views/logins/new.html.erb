<div class="top-hero-wrapper">
  <div class="container top-hero-container">
    <div class="row">
      <div class="col-md-offset-4 col-md-4 users-new-container">
        <h1 class="text-center">Log in</h1>
        <%= form_for :session, url: logins_path do |f| %>
          <div class="form-group">
            <%= f.label :account_name, class: "text-white" %>
            <%= f.text_field :account_name, class: "form-control" %>
          </div>
          <div class="form-group">
            <%= f.label :password, class: "text-white" %>
            <%= f.password_field :password, class: "form-control" %>
          </div>

          <%= f.submit "ログイン", class: "btn-block btn-white" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
